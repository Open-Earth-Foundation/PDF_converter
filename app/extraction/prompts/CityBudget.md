Context: Budget items in Climate City Contract documents (financial tables for climate measures).

- Capture year, total_amount, and currency exactly as shown; include description/notes if the table provides them.
- Only store rows with explicit numeric amounts; if IDs are missing, leave them blank (placeholders will be assigned).
- Capture any additional useful information not covered by other fields in the `misc` field as a JSON object.

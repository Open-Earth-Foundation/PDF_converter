Context: Targets defined in the Climate City Contract (e.g., emission reductions, renewable share, energy savings).

- Capture indicator linkage, target year, target value, and baseline details when explicitly stated.
- Preserve status/notes if present; omit IDs if missing (placeholders will be assigned). Skip only when no year or value is provided.
- Capture any additional useful information not covered by other fields in the `misc` field as a JSON object.

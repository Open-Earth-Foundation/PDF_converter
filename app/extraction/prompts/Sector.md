Context: Sectors used for GHG accounting (BISKO) or action portfolio grouping within Climate City Contracts.

- Capture sector names and any descriptions exactly as written (e.g., energy, buildings, mobility, industry, AFOLU).
- Only add notes if the document includes them; do not invent or normalize names beyond what appears.
- Capture any additional useful information not covered by other fields in the `misc` field as a JSON object.

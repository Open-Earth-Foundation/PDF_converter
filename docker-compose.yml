services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: pdf_converter
      POSTGRES_USER: pdf_user
      POSTGRES_PASSWORD: pdf_pass
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
